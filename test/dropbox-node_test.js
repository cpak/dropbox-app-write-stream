'use strict';

var dropboxNode = require('../');
var assert = require('should');

describe('dropboxNode', function () {

  it('should be awesome', function () {
    dropboxNode().should.equal('awesome');
  });

});
